import style from './InputForm.module.scss'
import classNames from 'classnames/bind'

let cx = classNames.bind(style)
const InputForm = ({clickOK, clickCancel}) => {
    return (
        <div className={cx("container")}>
            
        </div>
    )    
}